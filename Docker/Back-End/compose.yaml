services:
  backend:
    image: brunoyujitaka/proj_ext_backend:latest
    environment:
      NOME: "root"
      SENHA: "venuste"
      BUCKET: "bucket-imagem-teste"
      DB_HOST: "10.0.0.200"
      DB_PORT: "3307"
    ports:
      - "8080:8080"